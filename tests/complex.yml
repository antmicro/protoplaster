includes:
  - basic.yml

tests:
  network_test:
  - network:
      interface: "eth0"
  memory_test:
  - memtester:
      memory: "10M"
      iterations: 2

test-suites:
  full:
    tests:
    - local
    - network_test
    - memory_test
